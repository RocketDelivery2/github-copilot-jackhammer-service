import { codexReviewerPromptInventory, CodexReviewerPromptInventoryItem } from './codexReviewerPromptInventory'; import * as fs from 'fs';  export class CodexReviewerPromptLoader {   public listPromptIds(): string[] {     return codexReviewerPromptInventory.map(item => item.id);   }    public getPromptInfo(id: string): CodexReviewerPromptInventoryItem {     const item = codexReviewerPromptInventory.find(item => item.id === id);     if (!item) {       throw new Error(Unknown prompt id: );     }     return item;   }    public loadPrompt(id: string): string {     const info = this.getPromptInfo(id);     if (!fs.existsSync(info.repoPath)) {       throw new Error(Prompt file not found: );     }     return fs.readFileSync(info.repoPath, 'utf8');   } } 
