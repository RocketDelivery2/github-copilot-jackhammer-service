const taxonomy = require('../../src/agents/softwareCompanyAgentTaxonomy');  describe('Software Company Agent Department Coverage', () => {   it('should export department definitions', () => {     expect(taxonomy.departments).toBeDefined();     expect(Array.isArray(taxonomy.departments)).toBe(true);     expect(taxonomy.departments.length).toBeGreaterThan(0);   });    it('should have unique department names', () => {     const names = taxonomy.departments.map(d => d.name);     const unique = new Set(names);     expect(unique.size).toBe(names.length);   });    it('should cover required roles and capabilities for each department', () => {     taxonomy.departments.forEach(dep => {       expect(dep.roles).toBeDefined();       expect(Array.isArray(dep.roles)).toBe(true);       expect(dep.roles.length).toBeGreaterThan(0);       dep.roles.forEach(role => {         expect(role.capabilities).toBeDefined();         expect(Array.isArray(role.capabilities)).toBe(true);       });     });   });    it('should specify approval requirements for each department', () => {     taxonomy.departments.forEach(dep => {       expect(dep.approvalRequired).toBeDefined();       expect(typeof dep.approvalRequired === 'boolean' || typeof dep.approvalRequired === 'string').toBe(true);     });   }); }); 
