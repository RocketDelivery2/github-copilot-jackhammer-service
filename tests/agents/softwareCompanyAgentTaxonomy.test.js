const taxonomy = require('../../src/agents/softwareCompanyAgentTaxonomy');  describe('softwareCompanyAgentTaxonomy', () => {   it('should contain valid departments', () => {     const deps = taxonomy.map(a => a.department);     expect(new Set(deps)).toEqual(new Set(['Engineering','Quality Assurance','Security','Product']));   });   it('should flag high risk agents for human approval', () => {     const highRisk = taxonomy.filter(a => a.riskTier==='high');     expect(highRisk.every(a => a.humanApprovalRequired)).toBe(true);   });   it('should include engineer with correct capabilities', () => {     const eng = taxonomy.find(a => a.id==='engineer');     expect(eng.capabilities).toEqual(expect.arrayContaining(['code','review','test']));   }); });
